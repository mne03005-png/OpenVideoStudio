## What does this PR do?


## Related issue
Closes #

## Which community track, if any?
See `docs/COMMUNITY_TRACKS.md` — n/a if this is a core fix.

## Testing
- [ ] `python -m pytest tests/ -q` passes
- [ ] New behavior has test coverage (or explain why it can't)
- [ ] Manually verified against live services, if this touches a
      provider/pipeline stage (describe how)

## Does this change a shared interface?
(`providers/base.py`, `CreativeRunState`, the review-gate mechanics, or
anything under `studio/core/` shared with Media Remix) — if yes, link the
Discussion where this was raised first, per `CONTRIBUTING.md`.

## Checklist
- [ ] Commits are signed off (`git commit -s`) — see `CONTRIBUTING.md`'s
      DCO section
- [ ] No secrets, absolute local paths, or personal information introduced
